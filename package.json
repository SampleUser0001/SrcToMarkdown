{
  "name": "srctomarkdown",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "clean": "rm -fR ./lib",
    "build": "npm run clean && mkdir ./lib && cp ./src/* ./lib",
    "exec": "node ./lib/index.js $npm_config_src $npm_config_depth",
    "test": "jest src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "date-utils": "^1.2.21",
    "jest": "^29.5.0"
  }
}
